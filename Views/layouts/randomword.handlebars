<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mad Libs</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-giJF6kkoqNQ00vy+HMDP7azOuL0xtbfIcaT9wjKHr8RbDVddVHyTfAAsrekwKmP1" crossorigin="anonymous">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Pangolin&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
    <div class="container"></div>
    <div class="wrapper">
        <header class="sparkle">
            <nav class="navbar navbar-expand-lg sparkle">
                <div class="container-fluid">
                    <div class="" id="navbarSupportedContent">
                        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                            <li class="nav-item">
                                <a class="nav-link active" aria-current="page" href="/">HOMEPAGE</a>
                            </li>
                            {{#if loggedIn}}

                            <li class="nav-item">
                                <a class="nav-link get-all-story nav-link-play" id="completed" href="/dashboard">
                                    PLAY</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" id="completed" href="/">WORD BANK</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" id="completed" href="/dashboard/saved">VIEW SAVED</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" id="logout" href="/">LOGOUT</a>
                            </li>
                            {{else}}
                            <li class="nav-item">
                                <a class="nav-link" href="/login">LOGIN</a>
                            </li>
                            {{/if}}
                        </ul>
                    </div>
                </div>
            </nav>
            <h1>
                {{#if loggedIn}}
                <div id="checkbox-template" style="display:none">
                    <div style="display:inline-block">
                        <input type="checkbox" id="checkbox_{{adjectives}}" />
                        <label for="checkbox_{{adjectives}}">

                        <input type="checkbox" id="checkbox_{{adverbs}}" />
                        <label for="checkbox_{{adverbs}}">

                        <input type="checkbox" id="checkbox_{{nouns}}" />
                        <label for="checkbox_{{nouns}}">

                        <input type="checkbox" id="checkbox_{{verbs}}" />
                        <label for="checkbox_{{verbs}}">
                        {{label}}
                        </label>
                    </div>
                </div>
            </h1>

        </header>
        <main>
            {{{ body }}}
        </main>
    </div>
    </div>
    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}

</body>

</html>