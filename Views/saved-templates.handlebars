<div class="saved-template-bg">
<div class="container saved-note-pad">
<div class="saved-container">
{{#each story}}
<p class="fs-2 saved-title">{{title}}</p>
<p class="fs-4 saved-paragraph">{{text}}</p>
<button type = "input" class="btn btn-primary btn-sm delete-story-btn" value= "value">Delete Story</button>
{{/each}}

</div>
</div>
</div>
<script src="/javascript/delete-story.js"></script>